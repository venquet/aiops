# AIOps CRM

Phase 1 contains a small Spring Boot CRM service in `app/crm`. It provides customer and order APIs, PostgreSQL persistence, database migrations, health and metrics endpoints, and a container build. Azure and Kubernetes are intentionally not part of this phase.

See [app/crm/README.md](app/crm/README.md) for local usage.
